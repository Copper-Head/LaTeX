% This command loads the whatever preamble file is relevant to this document.
\input{preamble.tex}

% You can also specify document-specific commands here so that it's clearer where custom commands are coming from.

\begin{document}


\end{document}